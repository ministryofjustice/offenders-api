%h2
  Last import

- unless last_import
  %p
    No previous imports found.

- else
  %table
    %thead
      %tr
        %th
          File
        %th
          Date
    %tbody
      %tr
        %td
          = last_import.offenders_file.file.filename
        %td
          = last_import.created_at.strftime('%d/%m/%Y %H:%M:%S')
